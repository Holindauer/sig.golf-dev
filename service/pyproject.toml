[project]
name = "sig-golf-service"
version = "0.1.0"
description = "sig.golf: the site, the pull-request webhook and the verification worker for the leanSPHINCS challenge"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy>=2.0",
  "jinja2>=3.1",
  "httpx>=0.27",
  "markdown>=3.6",
  "nh3>=0.2.18",
]

[tool.uv]
package = false
